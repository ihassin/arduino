source 'https://rubygems.org'

gem 'arduino_firmata'
gem 'simplecov',          :require => false, :group => [:test, :staging]
gem 'rspec',              :group => [:test, :staging]
gem 'rspec-mocks',        :group => [:test, :staging]
gem 'cucumber',           :require => false, :group => [:test, :staging]
